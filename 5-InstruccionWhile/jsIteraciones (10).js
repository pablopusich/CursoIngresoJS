function mostrar()
{

	var contador=0;
	//declarar contadores y variables 
	
	var respuesta= true;

	while(respuesta)
	{
		var numero = prompt("ingrese un numero");
		numero = parseInt(numero);

		while(isNaN(numero)) {
			numero = prompt ("error numero ivalido");
				numero = parseInt(numero);
			}

	// calculo			
		respuesta = confirm("desea continuar?");
	
	}

	if (nume)




}//FIN DE LA FUNCIÓN